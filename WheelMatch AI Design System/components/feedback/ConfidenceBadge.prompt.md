The signature anti-hallucination trust marker. Attach to every AI-asserted skill/fact.

```jsx
<ConfidenceBadge state="confirmed" />   // Confirmé dans le CV
<ConfidenceBadge state="pending">Kubernetes à confirmer</ConfidenceBadge>
<ConfidenceBadge state="absent" />      // Absent du CV
```
