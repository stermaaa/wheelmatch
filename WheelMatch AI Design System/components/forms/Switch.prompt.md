Toggle for binary options — anonymisation, remote, premium. `onChange(next)` gives the boolean directly.

```jsx
<Switch label="Anonymiser le profil" checked={anon} onChange={setAnon} tone="ai" />
```
