Tab navigation. `underline` for page sections, `segmented` for compact in-panel switches (CV length, ton).

```jsx
<Tabs variant="segmented" value={v} onChange={setV} tabs={[{id:'one',label:'One-pager'},{id:'two',label:'2 pages'}]} />
```
