Surface container for panels, KPI tiles, profile cards. Use `interactive` for clickable cards (shortlist rows) and `selected` for the chosen item.

```jsx
<Card interactive selected={id === active} onClick={...}>…</Card>
```
