Icon-only square button. Pass a Lucide SVG as children and always set `label`.

```jsx
<IconButton label="Filtrer" variant="secondary"><FilterSvg/></IconButton>
```
Variants: ghost (toolbar default) · secondary (bordered) · subtle. Sizes sm/md/lg map to 32/38/44px hit targets.
