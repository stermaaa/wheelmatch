Candidate / user avatar. Shows photo if `src`, else deterministic colored initials. `status` adds an availability dot (available/busy/off).

```jsx
<Avatar name="Thomas Martin" size="lg" status="available" />
```
